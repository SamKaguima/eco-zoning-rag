# Task: [module name]

Create `src/[filename].py` with the following — no placeholders, fully implemented.

## Summary
[One sentence describing what this module does.]

## Functions

`function_name(param: Type, param: Type) -> ReturnType`
- What it does
- Specific behavior rules
- Edge cases to handle

## Imports
Only these imports should be used:
- 

## Main block
When run directly from project root (`python src/[filename].py`):
1. Load `.env` using python-dotenv
2. Instantiate clients using environment variables
3. [module-specific steps]

## What not to do
-