# Task: project scaffold

Create the following with no placeholders — fully implemented:

1. `src/__init__.py` — empty
2. `tests/__init__.py` — empty  
3. `requirements.txt` with: qdrant-client, openai, python-dotenv, pytest
4. `.env.example` with: OPENAI_API_KEY=your-key-here
5. `.gitignore` excluding: .env, .venv/, __pycache__/, *.pyc, data/raw/

Do not create any Python logic files yet. Confirm what was created.