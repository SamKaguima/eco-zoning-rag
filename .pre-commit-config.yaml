repos:
  - repo: local
    hooks:
      - id: pytest
        name: pytest
        entry: .venv\\Scripts\\python.exe -m pytest tests/ -v
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-commit, pre-push]
